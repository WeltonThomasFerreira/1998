import { FilterCategoryTypesPipe } from './filter-category-types-pipe';

describe('FilterCategoryTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategoryTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
